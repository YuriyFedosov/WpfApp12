<Window x:Class="WpfApp12.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp12"
        mc:Ignorable="d"
        Title="CheckBoxNew" Height="150" Width="300" ResizeMode="NoResize">
    <Window.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Rectangle Grid.Row="0" Stroke="LightGray" Width="15" Height="15" RadiusX="3" RadiusY="3"/>
                            <Ellipse x:Name="Null" Grid.Row="0" Fill="Red" Width="5" Height="5" Opacity="0"/>
                            <Path x:Name="Checked" Grid.Row="0" Stroke="Black" StrokeThickness="2" Opacity="0">
                                <Path.Data>
                                    <GeometryGroup>
                                        <PathGeometry Figures="M44,24 L53,34" />
                                        <PathGeometry Figures="M54,23 L43,34"/>
                                    </GeometryGroup>
                                </Path.Data>
                            </Path>
                            <ContentPresenter Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Center" Content="{TemplateBinding Content}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Checked" Property="Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="{x:Null}">
                                <Setter TargetName="Null" Property="Opacity" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <CheckBox Grid.Column="0" Content="checked" IsChecked="False"/>
        <CheckBox Grid.Column="1"  Content="unchecked" IsChecked="True"/>
        <CheckBox Grid.Column="2" Content="indeterminate" IsChecked="{x:Null}"/>
    </Grid>
</Window>
